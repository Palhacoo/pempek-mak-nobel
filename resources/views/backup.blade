<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <title>Dashboard Admin - Pempek Mak Nobel</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">

<div class="flex min-h-screen">

  <!-- Sidebar -->
  <aside class="w-60 bg-blue-600 text-white flex flex-col">
    <div class="p-6">
      <h1 class="text-xl font-bold leading-tight">Pempek<br>Mak Nobel</h1>
    </div>
    <nav class="flex-1">
      <ul class="space-y-2 px-4">
        <li>
          <a href="#" class="block bg-white/30 rounded px-4 py-2 font-semibold">Customer</a>
        </li>
        <li>
          <a href="#" class="block hover:bg-white/20 rounded px-4 py-2">Product</a>
        </li>
        <li>
          <a href="#" class="block hover:bg-white/20 rounded px-4 py-2">Transaction</a>
        </li>
      </ul>
    </nav>
  </aside>

  <!-- Main Content -->
  <main class="flex-1 p-8 bg-gray-100">

    <!-- Header Admin -->
    <div class="flex justify-end items-center mb-4">
      <span class="text-sm text-gray-600 mr-2">Admin</span>
      <div class="w-5 h-5 rounded-full bg-blue-600"></div>
    </div>

    <!-- Tombol Tambah Produk -->
    <div class="mb-6">
      <a href="{{ route('stok.create') }}" class="bg-green-600 text-white py-2 px-4 rounded hover:bg-green-700 inline-block">
        Tambah Produk
      </a>
    </div>

    <!-- Stok Produk -->
    <section class="mb-10">
      <h3 class="text-xl font-semibold text-gray-800 mb-4">Jenis Produk</h3>
      <div class="bg-white shadow rounded">
        <table class="min-w-full table-auto border-collapse text-gray-700">
          <thead class="bg-gray-100">
            <tr>
              <th class="px-4 py-2 border">Nama Produk</th>
              <th class="px-4 py-2 border">Harga</th>
              <th class="px-4 py-2 border">Aksi</th>
            </tr>
          </thead>
          <tbody>
            @foreach($stok as $item)
              <tr class="hover:bg-gray-50">
                <td class="px-4 py-2 border">{{ $item->nama_produk }}</td>
                <td class="px-4 py-2 border">Rp{{ number_format($item->harga, 0, ',', '.') }}</td>
                <td class="px-4 py-2 border">
                  <a href="{{ route('stok.edit', $item->id) }}" class="text-blue-600 hover:underline">Edit</a> |
                  <a href="{{ route('stok.delete', $item->id) }}" class="text-red-600 hover:underline">Delete</a>
                </td>
              </tr>
            @endforeach
          </tbody>
        </table>
      </div>
    </section>

    <!-- Order Section -->
    <section class="mb-10">
      <h3 class="text-xl font-semibold text-gray-800 mb-4">Order</h3>
      <div class="bg-white shadow rounded">
        <table class="min-w-full table-auto text-gray-700">
          <thead class="bg-gray-100">
            <tr>
              <th class="px-4 py-2 border">Nama Pelanggan</th>
              <th class="px-4 py-2 border">Nama Produk</th>
              <th class="px-4 py-2 border">Jumlah</th>
              <th class="px-4 py-2 border">Total</th>
              <th class="px-4 py-2 border text-center">Status</th>
            </tr>
          </thead>
          <tbody>
            @foreach ($orders as $order)
              <tr class="hover:bg-gray-50">
                <td class="px-4 py-2 border">{{ $order->nama_pelanggan ?? '-' }}</td>
                <td class="px-4 py-2 border">{{ $order->nama_produk }}</td>
                <td class="px-4 py-2 border">{{ $order->jumlah }}</td>
                <td class="px-4 py-2 border">Rp{{ number_format($order->harga * $order->jumlah, 0, ',', '.') }}</td>
                <td class="px-4 py-2 border text-center">
                  <input type="checkbox" class="form-checkbox">
                </td>
              </tr>
            @endforeach
          </tbody>
        </table>
      </div>
    </section>

    <!-- Customer List -->
    <section>
      <h3 class="text-xl font-semibold text-gray-800 mb-4">Daftar Pelanggan</h3>
      <div class="bg-white shadow rounded">
        <table class="min-w-full table-auto text-gray-700">
          <thead class="bg-gray-100">
            <tr>
              <th class="px-4 py-2 border">Nama Pelanggan</th>
              <th class="px-4 py-2 border">Alamat</th>
              <th class="px-4 py-2 border">No Telepon</th>
            </tr>
          </thead>
          <tbody>
            @foreach ($orders as $order)
              <tr class="hover:bg-gray-50">
                <td class="px-4 py-2 border">{{ $order->nama_pelanggan ?? '-' }}</td>
                <td class="px-4 py-2 border">{{ $order->alamat }}</td>
                <td class="px-4 py-2 border">{{ $order->no_telpon }}</td>
              </tr>
            @endforeach
          </tbody>
        </table>
      </div>
    </section>

    <!-- Logout -->
    <div class="mt-10 text-center">
      <a href="/logout" class="bg-red-600 text-white px-4 py-2 rounded hover:bg-red-700">Logout</a>
    </div>

  </main>

</div>

</body>
</html>
-------------------------------------------------------------------------------

create

<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Tambah Produk - Pempek Mak Nobel</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

    <div class="container mx-auto mt-10 p-6 bg-white shadow-md rounded-lg">
        <h2 class="text-2xl font-bold mb-6 text-center text-blue-600">Tambah Produk</h2>

        <form action="{{ route('stok.store') }}" method="POST">
            @csrf
            <div class="mb-4">
                <label for="nama_produk" class="block text-sm font-medium text-gray-700">Nama Produk</label>
                <input type="text" id="nama_produk" name="nama_produk" class="mt-1 p-2 w-full border rounded" required>
            </div>

            <div class="mb-6">
                <label for="harga" class="block text-sm font-medium text-gray-700">Harga</label>
                <input type="number" id="harga" name="harga" class="mt-1 p-2 w-full border rounded" required>
            </div>

            <button type="submit" class="w-full bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700">
                Tambah Produk
            </button>
        </form>
    </div>

</body>
</html>
-----------------------------------------------------------------------------------------------
edit

<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Edit Produk - Pempek Mak Nobel</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

    <div class="container mx-auto mt-10 p-6 bg-white shadow-md rounded-lg">
        <h2 class="text-2xl font-bold mb-6 text-center text-blue-600">Edit Produk</h2>

        <form action="{{ route('stok.update', $stok->id) }}" method="POST">
            @csrf
            <div class="mb-4">
                <label for="nama_produk" class="block text-sm font-medium text-gray-700">Nama Produk</label>
                <input type="text" id="nama_produk" name="nama_produk" value="{{ $stok->nama_produk }}" class="mt-1 p-2 w-full border rounded" required>
            </div>

            <div class="mb-6">
                <label for="jumlah_stok" class="block text-sm font-medium text-gray-700">Jumlah Stok</label>
                <input type="number" id="harga" name="harga" value="{{ $stok->harga }}" class="mt-1 p-2 w-full border rounded" required>
            </div>

            <button type="submit" class="w-full bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700">
                Update Produk
            </button>
        </form>
    </div>

</body>
</html>
